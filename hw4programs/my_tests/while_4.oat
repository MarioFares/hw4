int program(int argc, string[] argv) {
    var x = 10;
    var y = 0;

    var sum = 0;

    while (x > 5 & y < 5) {
        sum = sum + x + y;
        x = x - 1;
        y = y + 1;
    }

    return sum;
}