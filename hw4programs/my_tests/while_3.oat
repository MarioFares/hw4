int program(int argc, string[] argv) {
    var x = 10;
    var y = 0;

    var sum = 0;
    while (sum < 50) {
        sum = sum + x + y;
        x = x - 1;
        y = y + 1;
    }   

    return sum;
}